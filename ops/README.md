# ops/ — Operational artifacts

| File | Purpose |
|---|---|
| `diagnosis.md` | Latest `ruff` / `pytest` / `npm build` + `docker compose config` + `curl /health` output — re-runnable |
| `deployment.md` | Render deployment proof template (fill after `render.yaml` deploy) |
| `runbook.md` | Day-2 ops: logs, backup, rotate secrets, incident |

